package commands

func RemoveTracks(trackIds []string) error {
	// 1. Remove tracks from playlist

	// 2. Remove track from database(s) - practically speaking, this will just be a redis cache

	return nil
}
